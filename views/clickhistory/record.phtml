<?php
	declare(strict_types=1);
	/** @var ClickHistoryView $this */
	// The click listener ignores the body and only ever looks at whether the
	// request went through, but an endpoint that answers with an empty document
	// is a pain to debug from the network tab.
	echo json_encode($this->result);
